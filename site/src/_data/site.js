/**
 * Global site data — the production URL, needed to build absolute canonical
 * and Open Graph URLs (those must be absolute per the OG spec; relative
 * paths are not valid there).
 */
module.exports = {
	url: 'https://activity-log.reslab.pro',
};
